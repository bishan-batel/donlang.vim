# main method (args parameter optional)


extern cos(n: f64): f64

extern putchar(str: char): i32
extern puts(str: string): i32
extern printf(str: string): i32

def main(): i32 {
    printf("Bruh Moment\n")

    let a = 420

    let b = ptr a

    return 0
}

